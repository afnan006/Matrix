module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Include all source files for Tailwind processing
  ],
  theme: {
    extend: {}, // Extend Tailwind's default theme here if needed
  },
  plugins: [], // Add any Tailwind plugins here if needed
};
